#Importing matplotlib and numpy Libraries
import matplotlib.pyplot as plt
import numpy as np

#Giving the values for the x and y axis
x=np.array([0,25])
y=np.array([0,30])

#It will plot the data givenn in the array
plt.plot(x,y)

#To display the plot in teh window
plt.show()

